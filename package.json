{
  "name": "process-statsd",
  "version": "1.0.5",
  "description": "send nodejs process metrics to graphite",
  "repository": {
    "type": "git",
    "url": "https://github.com/oss-ethinking/process-statsd"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc --project .",
    "dev": "concurrently \"tsc --project . --watch\" \"nodemon dist\"",
    "test": "jest"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "@types/node": "^11.15.10",
    "lynx": "^0.2.0",
    "lynx-express": "^0.2.0"
  },
  "devDependencies": {
    "@types/jest": "^24.9.1",
    "concurrently": "^4.1.2",
    "jest": "^24.9.0",
    "nodemon": "^1.19.4",
    "rimraf": "^2.7.1",
    "ts-jest": "^24.3.0",
    "ts-node": "^8.8.2",
    "typescript": "^3.8.3"
  }
}
